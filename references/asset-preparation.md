# 素材准备与资产目录

在设计大纲前建立可追溯的候选索引。原始解析结果只留在来源缓存；正式资产目录只接收入选或用户明确要求交付的素材。

## 目录

- [建议目录](#建议目录)
- [论文图片提取](#论文图片提取)
- [MinerU 候选资产](#mineru-候选资产)
- [图片说明文档](#图片说明文档)
- [表格和数据图表](#表格和数据图表)
- [公式](#公式)
- [校徽与学校品牌](#校徽与学校品牌)
- [资产选择与加工](#资产选择与加工)

## 内部工作目录

```text
project/
├── source/
│   └── normalized/
│   │   ├── document-index.json
│   │   ├── blocks.ndjson
│   │   ├── page-map.json
│   │   ├── figure-candidates.json
│   │   ├── table-candidates.json
│   │   ├── formula-candidates.json
│   │   └── extraction-record.json
├── assets/
│   ├── figures/
│   │   ├── original/
│   │   ├── ready/
│   │   ├── figures.manifest.json
│   │   ├── 论文图片说明.md
│   │   └── figures-contact-sheet.png
│   ├── tables/
│   ├── formulas/
│   └── branding/
│       ├── logo-original.svg
│       ├── logo.svg
│       ├── logo.png
│       └── logo.meta.json
├── evidence-index.json
├── phase-metrics.json
├── project-config.json
└── deck-spec.json
```

以上目录是 Codex 内部工作区，不是客户交付结构。供应商原始包与远程响应应放在可忽略的缓存目录，而不是 `assets/`。`source/`、manifest、项目配置、deck spec、deck map、证据索引、阶段指标、联系表、logo 元数据和分析文件不得整目录复制给客户。

客户包只保留 `references/delivery.md` 规定的 `assets/`。先在内部建立一个“已筛选素材目录”，再交给 staging 脚本；不要直接传入完整工作资产库。交付侧按需创建 `figures/`、`formulas/`、`branding/` 和 `data/`；空子目录不创建。论文/单篇文献项目只有在用户明确要求时才交付完整忠实图集与 `论文图片说明.md`，并把它们计入显式选择集；多篇文献组会默认只交付入选和重点分析图片。机器 manifest、联系表、公式临时 PDF、编译日志、品牌元数据、未使用素材和品牌候选不交付。交付 `data/` 只允许本稿真正依赖的 CSV/XLSX；内部 JSON 数据应在 MJS 中嵌入或转成受控表格，不直接散发。

论文图、表格、公式、校徽和装饰素材必须分开。不得覆盖 `original/`；裁白边、增强、标注、拆分或格式转换的结果放入 `ready/` 并记录转换过程。

正式构建只引用项目内已核验的本地资产。网页图片、出版页面截图和校徽先下载到对应资产目录并记录来源、访问日期与哈希；不要让最终 PPTX 在构建或播放时依赖远程图片 URL。

## 论文图片提取

1. 识别图注、图号、页码和图前图后正文。
2. 优先提取 PDF 中的原始嵌入位图或可保真的矢量内容。
3. 遇到复合矢量、叠加文字、箭头、无法独立提取的多层图时，使用高分辨率页面裁切。
4. 图片本体通常不包含论文图注和正文；图内坐标轴、图例、箭头、子图编号和必要标签应保留。
5. 同一图号的多面板图默认保持整体；同页不同图号分别提取。
6. 用稳定 ID 和“图号 + 简短图名”命名；消除路径非法字符，但不要改变论文图号。
7. 计算尺寸、格式、哈希和清晰度，按 `schemas/figures-manifest.schema.json` 建立清单。
8. 校验“识别到的图注数、manifest 记录数、实际文件数”，解释所有差异。

低清、缺图例、裁切不完整或疑似引用外部资料的图片标记复核。不要默认使用生成式修复补出不存在的细节。

## MinerU 候选资产

只有在用户明确授权上传或提供现成 MinerU 解压结果时，才按 `references/mineru-source.md` 使用 MinerU。解析器输出的图、表和公式都是候选，不是已核验资产：

1. 先读取 `document-index.json` 和候选清单，不读取完整 `full.md`、`content_list`、`model`/`layout`、HTML、TeX 或整个 `blocks.ndjson`；
2. 通过页码、候选 ID、类型或关键词 hydrate 当前叙事需要的证据；
3. 仅把入选候选复制或忠实重裁到 `assets/**/original/`，再为实际页面需要建立 `ready/` 变体；
4. 用源 PDF 核对关键数字、单位、公式上下标、表格列关系、图注归属和多面板组合；
5. MinerU 小图不满足投影清晰度时，从源 PDF 高分辨率重裁，不做生成式补细节。

同页多个图片块可能属于一张多面板图。结合图号、图注、bbox、阅读顺序和源页视觉检查后再决定整体保留或拆分；不得把解析器的块边界当作论文语义边界。归一化候选 ID 保持稳定，正式 manifest 记录其候选 ID、源页、原始 bbox、验证状态和后续转换。

## 图片说明文档

从 manifest 生成 `论文图片说明.md`，避免人读文档与机器清单分叉。每图保持简洁，但必须结合图注及上下文，不可只看图片猜测：

```markdown
## 图3.19 锁滚轮运动轨迹

![图3.19](original/图3.19 锁滚轮运动轨迹.png)

- 论文位置：第3章第3.3节；PDF第__页；论文页码第__页
- 图片内容：图中可直接观察到的对象和关系
- 在论文中的作用：它在论证链中承担什么任务
- 可支撑结论：与证据索引连接的结论，不夸大因果
- PPT使用价值：核心/辅助/附录/不建议
- 建议呈现：裁切、对比、标注、图例或布局建议
- 注意事项：清晰度、图例、单位、版权或解释风险
- 来源性质：论文原创结果/论文引用资料/无法确定
```

## 表格和数据图表

- 优先提取为 CSV、JSON 或可编辑表格；保留截图作为校对备份。
- 记录表号、标题、单位、脚注、样本口径和论文页码。
- 只有在数据可复核时重绘图表；保留输入数据、变换和计算说明。
- 不因视觉整洁删除会改变解释的误差线、显著性、样本量、单位或基准组。
- 大型表格应提炼主结果，完整表放附录。

## 公式

- 先建立公式索引，记录公式号、原始符号、页面、用途和上下文。
- 只渲染大纲已选中的核心公式；`.tex` 是唯一权威源，PDF 只是临时中间件。
- 将论文或模型生成的 LaTeX 视为不可信数据，只允许纯数学表达式。先运行 `scripts/render-formula.mjs --latex <表达式> --validate-only`；不得绕过其数学命令允许列表、危险控制序列拒绝和 `-no-shell-escape` 编译边界。
- 本机存在 LaTeX 时，优先用 LaTeX/PDFLaTeX 编译；表达式确有 Unicode 或中文时使用 XeLaTeX。由同一源一次生成路径化 SVG 与透明高分辨率 PNG。
- 使用 `scripts/render-formula.mjs --latex <表达式> --output-dir <assets/formulas> --name <稳定名称>`，不要为每篇论文重复编写公式编译脚本。
- 目标 PowerPoint 已验证支持 SVG 时使用 SVG；目标环境未知、旧版软件或 SVG 验证失败时，直接把 `asset_ref` 切换为 PNG。不要依赖 OOXML 自动生成的栅格 fallback。
- 本机没有 LaTeX 时，论文已有公式优先从源 PDF 做高分辨率忠实裁取；新公式只使用当前环境已经具备且可信的本地 MathJax/KaTeX 渲染器。短而非核心的表达式可使用逐字符核对的 Unicode 数学文本。
- 不得使用 raw LaTeX、搜索结果公式图片或未经验证的在线公式图片服务作为页面 fallback。复杂核心公式无法可靠渲染时停止并报告；非核心公式则移除或放入备注。
- 逐字核对上下标、希腊字母、括号、编号和量纲。
- 公式渲染不改变数学含义；解释文字与论文术语保持一致。
- 仅为排版可读性重排换行，不擅自简化或补全推导。
- 自动回退最多允许一次合理的 LaTeX 引擎切换和一次 SVG→PNG 切换；不要无限尝试工具。

## 校徽与学校品牌

检索顺序：

```text
识别学校和校区
→ 检查用户提供的官方素材或项目级已核验 catalog
→ 没有可用素材时搜索学校官网的品牌、视觉识别或下载页
→ 核验学校、校区、当前名称、素材类型和版本
→ 保存原文件与来源
→ 生成规范化 SVG 和透明 PNG
→ 在白底与深底上验证
```

本 Skill 不随包分发大学校徽。网络检索优先学校官网的视觉识别系统、品牌手册或官方下载页；不要直接使用搜索结果缩略图、来源不明聚合站或自行仿制的校徽。

如项目已有带来源的校徽目录，可使用 `scripts/find-university-logo.mjs <学校正式名> --catalog <项目内 catalog.json>` 查询。默认只接受标准化后的精确名称或别名。只有人工排查旧名、简称或拼写差异时才使用 `--fuzzy`；模糊结果始终是待核验候选，绝不能因名称相似直接放入 PPT。

项目内 `logo.meta.json` 至少记录：`institution_name`、`aliases`、`campus`、`asset_type`、`source_url`、`retrieved_at`、`sha256`、`transformations`、`usage_notes` 和 `verification_status`。未经核验的素材不得放入 PPT，也不自动写入 Skill 仓库。

不要擅自改色、改变比例、删掉校徽内部白色、添加阴影或重绘细节。找不到可信素材时使用学校正式文字标识，并在交付说明中标记；品牌素材缺失不得改变用户已经选择的主题预设。

## 资产选择与加工

- 在大纲阶段先选择稳定资产 ID，再制作 `ready/` 版本。除显式完整图集外，`ready/` 的规范资产数不得大于入选资产数；一个入选资产可以因已记录的兼容或版式需要拥有少量变体。
- 论文原创图与论文引用图必须区分；后者不能写成作者原创成果。
- 标注只突出已有证据，不添加论文未支持的结论。
- 裁切、旋转、增强、重绘和格式转换全部进入 provenance。
- 视觉素材应有 alt text 或可供讲稿使用的内容说明。
- 同一资产在多页复用时保持颜色、标注含义和裁切逻辑一致。
- 每次资产阶段结束时记录候选数、入选数、实际加工数、未引用 ready 变体数、缓存命中和耗时；未引用变体必须删除项目引用或记录合法保留理由，不继续批量加工。
